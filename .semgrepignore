# This duplicates `sast_excluded_paths` on the dsor-compliance component in
# .gitlab/ci/compliance.yml, because that input is not reaching the analyzer.

tests/
verification/
docs/
.gitlab/
.github/
.nox/
.venv/
